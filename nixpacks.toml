[variables]
  ASPNETCORE_URLS = "http://0.0.0.0:8080"

[phases.setup]
nixPkgs = ["dotnet-sdk_8"]

[phases.install]
cmds = ["dotnet restore"]

[phases.build]
cmds = ["dotnet publish -c Release -o out"]

[start]
cmd = "dotnet out/SharkStyleApi.dll"
